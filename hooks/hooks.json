{
  "description": "Cache-aware cost guard — warns before submitting prompts that may trigger expensive cache rewrites",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/dist/prompt-hook.cjs",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
