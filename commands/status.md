---
name: status
description: Show cache impact tracking stats for the current session
---

Run `echo __CACHE_CONTROL_STATUS__` to display session cache impact statistics. The cache-control hook will intercept this command and return the current session's token usage tracking data.
